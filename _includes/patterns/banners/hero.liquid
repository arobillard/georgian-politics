<header class="banner-hero" {% if include.img %}style="background-image: linear-gradient(to bottom, rgba(0, 0, 0, .5), rgba(0, 0, 0, .5)), url('{{site.baseurl}}{{include.img}}')"{% endif %}>
  <div class="banner-content island">
    {% if include.headline %}
    {% if page.url == '/' %}
      <strong class="block text-fff tena">
    {% else %}
      <h1 class="push-0 text-fff tena">
    {% endif %}
      {{ include.headline }}
    {% if page.url == '/' %}
      </strong>
    {% else %}
      </h1>
    {% endif %}
    {% endif %}
    {% if include.byline %}<p class="giga">{{ include.byline }}</p>{% endif %}
  </div>
</header>
